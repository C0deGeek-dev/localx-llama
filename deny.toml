[advisories]
ignore = [
    # Transitive through reqwest/rustls. This is an unmaintained warning, not
    # a known vulnerability. Owner: c0degeek-dev. First observed: 2026-08-30.
    # Disposition: accept indefinitely — no direct control over this
    # transitive dependency; revisit if reqwest/rustls drops it.
    "RUSTSEC-2025-0134",
]

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "CDLA-Permissive-2.0",
    "Zlib",
    "PolyForm-Noncommercial-1.0.0",
]

[bans]
multiple-versions = "warn"
wildcards = "warn"
allow-wildcard-paths = true

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
